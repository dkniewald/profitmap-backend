spring:
  mail:
    host: smtp.zoho.eu        # ili smtp.zoho.com ako nisi na EU data centru
    port: 587
    username: hello@profitmap.app
    password: "9%Xj4M6uKAi3!%x" #${MAIL_PASSWORD}    lozinka ili app password iz Zoho Maila
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          from: hello@profitmap.app  # Envelope sender must match SMTP username for Zoho

mail:
  from-address: "hello@profitmap.app"
  system-from-address: "hello@profitmap.app"
  app-name: "ProfitMap"
  activation-base-url: "https://app.profitmap.app/auth/activate"
  # Second Zoho mail account configuration (optional)
  second-account:
    host: smtp.zoho.eu
    port: 587
    username: no-reply@profitmap.app
    password: "hn4f@zvT" #${SECOND_MAIL_PASSWORD}
    from-address: "no-reply@profitmap.app"

